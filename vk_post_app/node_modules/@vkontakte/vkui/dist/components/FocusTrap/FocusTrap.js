'use client';
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import * as React from "react";
import { useFocusTrap } from "../../hooks/useFocusTrap/index.js";
import { DEFAULT_MUTATION_OBSERVER_OPTIONS } from "../../hooks/useMutationObserver.js";
export const FocusTrapInternal = ({ autoFocus = true, restoreFocus = true, disabled = false, mount = true, autoFocusDelay = 0, rootRef, children, mutationObserverOptions = DEFAULT_MUTATION_OBSERVER_OPTIONS })=>{
    const { beforeGuard, afterGuard } = useFocusTrap(rootRef, {
        autoFocus,
        restoreFocus,
        disabled,
        mount,
        autoFocusDelay,
        mutationObserverOptions
    });
    return /*#__PURE__*/ _jsxs(React.Fragment, {
        children: [
            beforeGuard,
            children,
            afterGuard
        ]
    });
};
export const FocusTrap = (props)=>{
    return /*#__PURE__*/ _jsx(FocusTrapInternal, {
        ...props
    });
};

//# sourceMappingURL=FocusTrap.js.map